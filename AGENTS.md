# 项目协作规则

默认使用简体中文回答，除非用户明确要求其他语言。

## BV 采集的搜索顺序

- 用户在本项目发起视频收集、搜索或补齐时，第一尝试使用 `firecrawl-search` 技能。先读取该技能的 `SKILL.md`，按其要求搜索 B站公开视频页面，并保存搜索证据。
- 先用 Firecrawl 覆盖目标分区的关键词；仅当服务不可用、额度不足、结果不足或缺少可靠的播放量／发布时间证据时，再使用项目现有 WBI 客户端核验或补充。切换前简要说明原因，不要默认先请求 WBI 再把 Firecrawl 当作兜底。
- 搜索摘要或搜索日期过滤不能代替逐条核验：保留最近滚动 90 天、播放量严格大于 10,000 的视频；证据不足的不计入完成数量。沿用历史 BV、已绑定 BV 和跨分区排重规则。
- 每个分区默认收集本批新增 50 条，标注实际采集日期；Midjourney、Stable Diffusion 不足时，用有趣、恶搞类 AI 成片补位，不用教程充当趣味补位，也不与 AI视频分区重复。
- 发布时当前分区只保留本批，旧记录归档并保留供下次排重；不足 50 条时如实报告缺口，不用历史记录充数。

默认使用 `app.mjs collect`。`scan`、`fill-courses` 和 `cycle` 也默认进入同一 Firecrawl 优先批次流程；不要向新同事推荐先执行旧 WBI 全量扫描。先检查 Firecrawl CLI 安装、登录和额度，再小批量试运行。只有用户明确选择旧模式时才使用 `--provider wbi`。

当天重复运行续补同一批次；完整采集后才允许同步飞书。安装定时任务不是首次使用的必要步骤；未经用户要求，不启用定时任务或更改全局技能。
